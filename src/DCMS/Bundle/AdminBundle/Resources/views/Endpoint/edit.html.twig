{% extends "DCMSAdminBundle:Endpoint:layout.html.twig" %}
{% block javascripts %}
    {{ parent() }}
    {% for javascript in epDef.javascriptDependencies('edit') %}
        <script type="text/javascript" src="{{ asset(javascript) }}"></script>
    {% endfor %}
{% endblock %}
{% block stylesheets %}
    {% for stylesheet in epDef.stylesheetDependencies('edit') %}
        <link rel="stylesheet" href="{{ asset(stylesheet) }}" />
    {% endfor %}
{% endblock %}

{% block endpoint_edit %}
    <form action="{{ path('dcms_admin_endpoint_edit', {'endpoint_uuid': endpoint.uuid }) }}" method="POST">
        <div class="row">
        <div class="ten columns">
            <h3 class="subheader">{{ epDef.title }} </h3>
        </div>
        <div class="two columns action-bar text-right">
            <input type="submit" value="Update" class="button"/>
        </div>
        </div>
        {{ form_errors(form) }}
        <div>
            {{ form_errors(form.path) }}
            {{ form_widget(form.path, {'attr': {'class': 'large'}}) }}
        </div>
        {% render epDef.controller('edit') with {
            'endpoint': endpoint,
            'form': form,
        } %}
        {{ form_rest(form) }}
    </form>
{% endblock %}
