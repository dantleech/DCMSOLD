<li>
    <a href="{{ node.path }}">{{ node.nodeName }}</a>
    {% if node.children|length > 0 %}
        <ul>
        {% for child in node.children %}
            {% include "DCMSMenuBundle:Block:_menuNode.html.twig" with {'node': child} %}
        {% endfor %}
        </ul>
    {% endif %}
</li>
