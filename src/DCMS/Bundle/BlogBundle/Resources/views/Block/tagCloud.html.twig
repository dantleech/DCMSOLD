<ul class="tagCloud">
{% for name, tag in wTags %}
    <li style="font-size: {{ 24 * tag.weight }}px">
        <a href="{{ path('dcms_blog_post_index', {'tag': tag }) }}">{{ tag }}</a>
    </li>
{% endfor %}
</ul>
