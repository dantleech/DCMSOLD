{% if node.showInMenu %}
<li>
    <a href="{{ node.fullPath }}">{{ node.nodeName }}</a>
    {% if node.children|length > 0 %}
        <ul>
        {% for child in node.children %}
            {% include "DCMSMenuBundle:Block:_menuNode.html.twig" with {'node': child} %}
        {% endfor %}
        </ul>
    {% endif %}
</li>
{% endif %}
